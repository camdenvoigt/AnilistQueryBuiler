RSpec.describe AnilistQueryBuilder do
  it "has a version number" do
    expect(AnilistQueryBuilder::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
